const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const u = await prisma.user.findFirst({ orderBy: { id: 'desc' } }); console.log('Latest User:', u); if (u && u.id > 1) { await prisma.user.update({ where: { id: u.id }, data: { isActive: !u.isActive } }); console.log('Update success'); await prisma.user.delete({ where: { id: u.id } }); console.log('Delete success'); } } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
